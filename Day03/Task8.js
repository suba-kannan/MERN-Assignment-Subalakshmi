const employee = {
    name: "Ravi",
    address: {
        city: "Chennai",
        pincode: 600001
    }
};

console.log("City:", employee.address.city);
console.log("Pincode:", employee.address.pincode);
