// What is a custom hook in React?
//  A custom hooks is a reusable function that allows you to share logic between components

// Why do we use custom hooks instead of writing logic directly in components?

// We acn reuse logic, gives clean code and avoid duplication